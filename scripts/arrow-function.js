function add(x, y) {
    return x + y
}
console.log(add(2,3));
const adding = (x, y) => x + y;
console.log(adding(10, 30));


//Arrow
// there is no def arguments in arrow function, where as default function has arguments

